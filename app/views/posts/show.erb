
<div class="post">
	<div class="title"> <%= @post.title %> </div>
	   <div class="date"> <%= time_ago_in_words(@post.created_at || Time.now) + ' ago' %>
	   	<br>
	   	   <%= @post.body %>

	   	   <p> <%= link_to 'View all posts', url_for(:posts, :index) %></p>